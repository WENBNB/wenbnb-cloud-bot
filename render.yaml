services:
  - type: web
    name: wenbnb-cloud-bot
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python wenbot.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.8
